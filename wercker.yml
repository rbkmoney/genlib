box: erlang:18
build:
    steps:
      - script:
        name: run test suite
        code: make test
    after-steps:
      - slack-notifier:
        url: ${SLACK_WEBHOOK_URL}
        username: "wercker"
